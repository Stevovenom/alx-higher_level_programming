#!/usr/bin/node
// prints 3 lines by using the data stored in array
const list = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < list.length; i++) {
  console.log(list[i]);
}
